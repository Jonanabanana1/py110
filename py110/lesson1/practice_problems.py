# 1) Determine number of occurrences of the string banana in the tuple
# fruits = ("apple", "banana", "cherry", "date", "banana")
# print(fruits.count("banana"))

# 2) Determine length of set without running code
# numbers = {1, 2, 3, 4, 5, 5, 4, 3}
# print(len(numbers))

# 3) Create a set containing both sets A and B
# a = {1, 2, 3, 4}
# b = {3, 4, 5, 6}
# c = a | b
# print(c)

# 4) What is the output without running the code
# names = ["Fred", "Barney", "Wilma", "Betty", "Pebbles", "Bambam"]
# name_positions = {}
# for index, name in enumerate(names):
#     name_positions[name] = index
# print(name_positions)

# 5) Calculate total age given the following dictionary
# ages = {
#     "Herman": 32,
#     "Lily": 30,
#     "Grandpa": 5843,
#     "Eddie": 10,
#     "Marilyn": 22,
#     "Spot": 237,
# }
# total_age = sum(ages.values())
# print(total_age)
# 7) Determine the minimum age in ages
# minimum_age = min(ages.values())
# print(minimum_age)

# 7) Determine output without running code:
# words = ["ant", "bear", "cat"]
# selected_words = []
# for word in words:
#     if len(word) > 3:
#         selected_words.append(word)

# print(selected_words)

# 8) Given the following string, create a dictionary representing frequency of each letter in the string
# statement = "The Flintstones Rock"
# letter_frequency = {}
# cleaned_statement = statement.replace(" ", "")

# for letter in cleaned_statement:
#     letter_frequency.setdefault(letter, 0)
#     letter_frequency[letter] += 1
# print(letter_frequency)

# 9) What is the return value for the list comprehension below
# [num for num in [1, 2, 3] if num > 1]

# 10) What does the code print and why?
# dictionary = {"a": "ant", "b": "bear"}
# print(dictionary.popitem())
